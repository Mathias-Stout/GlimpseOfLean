/- In `Mathlib.LinearAlgebra.Matrix.Transvection` it is proven that one can reduce a matrix
 to a diagonal form by only performing row and column operations (and without ever permuting them).

 State the corresponding theorem for row-echelon form instead of diagonal form.
 Start by defining row-echelon form and the sets of allowed transformations.
 If you feel particularly brave, you may attempt to prove (parts) of this theorem,
 drawing inspiration from `Mathlib.LinearAlgebra.Matrix.Transvection`.
 -/
